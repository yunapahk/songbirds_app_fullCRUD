<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Index</title>
</head>
<body>
    <h1>List of Animals</h1>

    <div class="create-button">
        <a href="animal/new"><button>Creat New Animal</button></a>
    </div>

    <% animals.forEach((pokemon, index) => { %>
    <% }) %>

    <div class="container">
        <a href="/animal/<%= animal._id %>"<h2><%=animal.name%></h2></a>
    </div>

</body>
</html>